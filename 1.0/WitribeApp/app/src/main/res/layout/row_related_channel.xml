<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_view"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    card_view:cardCornerRadius="8dp"
    card_view:cardUseCompatPadding="true"
    android:weightSum="1">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="@dimen/related_cell_height"
        android:layout_height="@dimen/related_cell_height"
        >

        <RelativeLayout
            android:id="@+id/rl_category_photo"
            android:layout_width="@dimen/related_cell_height"
            android:layout_height="@dimen/related_cell_image_height">

            <ImageView
                android:id="@+id/category_photo"
                android:layout_width="@dimen/related_cell_height"
                android:layout_height="@dimen/related_cell_image_height"
                android:contentDescription="@string/action_settings"
                android:scaleType="fitXY"
                android:src="@drawable/bg_placeholder"
                android:visibility="gone" />

            <ProgressBar
                android:id="@+id/pb_category_photo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:indeterminate="true" />
        </RelativeLayout>



        <TextView
            android:id="@+id/category_name"
            android:layout_width="@dimen/related_cell_height"
            android:layout_height="@dimen/related_cell_text_height"
            android:background="#1976D2"
            android:gravity="center_horizontal"
            android:text="@string/hello_world"
            android:textColor="#ffffff"
            android:textSize="13sp"
            android:lines="1" />
    </LinearLayout>
</android.support.v7.widget.CardView>